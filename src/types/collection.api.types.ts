export enum StatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
export enum VisibilityEnum {
  PUBLIC = "public",
  PRIVATE = "private",
}

export interface PostCollectionResponse {
  id: string;
}
